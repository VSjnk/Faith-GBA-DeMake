//currenty this code doesn't do much, just tests the position code.
#include "main.c"
#include "gba.h"

#include <math.h>


int main() {
    if(posX == 1 && pos Y == 1)
    {
        clearBackground(0, 0, 10);
    }
    
    return 0;
}
